# OmniAuth Connect ID Strategy

This Omniauth strategy is based of https://github.com/zquestz/omniauth-google-oauth2

## Installation

Add to your `Gemfile`:

```ruby
gem "omniauth-connectid"
```

Then `bundle install`.
